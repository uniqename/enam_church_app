import 'package:flutter/material.dart';

// TODO: Restore from claude.ai web conversation (March 27-28)
class ChildSermonsScreen extends StatelessWidget {
  const ChildSermonsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ChildSermonsScreen')),
      body: const Center(
        child: Text('Screen content to be restored'),
      ),
    );
  }
}
